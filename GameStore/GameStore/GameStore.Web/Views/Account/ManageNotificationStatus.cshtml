@model GameStore.Web.Models.ManagerProfileViewModel

@{
    ViewBag.Title = "ManageNotificationStatus";
}

<h2>ManageNotificationStatus</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ManagerProfileViewModel</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.NotifyStatus)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NotifyStatus)
            @Html.ValidationMessageFor(model => model.NotifyStatus)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Manage")
</div>
